# Project Context — Market Scope and Risk Universe

> Populated by `/onboard`. Replace `[populate via /onboard]` placeholders with your details. Inline examples show the intended flavor for the answers.

## Market / Product Scope

What market are we scoring? Be as specific as possible — "U.S. retail wheat flour, 5-lb consumer SKUs" beats "agriculture."

`[populate via /onboard]`

*Example: "Premium ground coffee, 12-oz bag SKUs, sold direct-to-consumer in CA/OR/WA, 14 SKUs across 3 brands."*

## Time Horizon

Over what window are these risks scored? Risk matrices conflate incomparable horizons silently — pin this down before scoring anything.

- Short term (next quarter, ≤ 90 days): `[populate via /onboard]`
- Medium term (1–4 quarters): `[populate via /onboard]`
- Long term (12–36 months): `[populate via /onboard]`

If you score a single matrix across mixed horizons, every output is misleading. The workspace will create separate matrices per horizon by default.

## Decision This Will Inform

What concrete decision is this risk register feeding?

`[populate via /onboard]`

*Example: "Q3 hedge ratio for green coffee futures; SKU-level price increase calendar for Q4."*

## Stakeholder Audience

Who reads the register? This drives the level of economic vocabulary in the output.

`[populate via /onboard]`

*Example: "VP Supply Chain (numerate, no econ background); CFO (financial vocabulary, no microeconomics); board risk committee (high-level only)."*

## Data Available for Empirical Estimation

Tick what's available. Influences whether the agent estimates elasticities empirically or uses literature priors.

- [ ] Internal price/quantity time series (window: `____`, granularity: `____`)
- [ ] Promotional history (price discounts + lift)
- [ ] Competitor pricing data
- [ ] Macro indicators (CPI, household income, GDP) for the geography
- [ ] Cost-of-goods history (input prices)
- [ ] None of the above — use literature priors only

`[populate via /onboard]`

## Known Watchlist Scenarios

The named scenarios you already worry about — these become the first set of register rows. Don't censor; the workspace will help you score them, including the ones you suspect are low risk.

`[populate via /onboard]`

*Example bullet list:*
- *Brazil drought reduces arabica supply 8–15%*
- *Recession depresses premium-segment demand*
- *Major retailer launches private label at -25% price point*
- *FDA/USDA new labeling mandate adds 3% COGS*

## Out of Scope

What's explicitly NOT being scored here? Documenting this prevents the matrix from being misread as comprehensive.

`[populate via /onboard]`

*Example: "Cyber/operational risk, FX exposure beyond pass-through, ESG investor sentiment, employment law changes."*
