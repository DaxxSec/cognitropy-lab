# Active Plan

> The single canonical plan file for the workspace. Pivot history (when a plan changes substantively) lives in `planning/pivots/`.

## Current Plan

(populated by `/onboard` and refined per session)

### Goal

(e.g. "Build a starter rule set across two productions over the next quarter, peer-reviewed two-distinct-roles, with calibration baselines for every puppet in inventory.")

### Phase 1 — Establish Baselines (week 1)

- [ ] Run `/calibration-audit` for every puppet in `context/project.md`
- [ ] Confirm peer-review roster has at least one reviewer per role bucket
- [ ] Capture three rehearsal logs in pure-narrative mode to test workflow
- [ ] Author the first draft rule (target: a pattern observed in two of those three logs)
- [ ] Run the first `/peer-review` pass

### Phase 2 — Build Initial Active Rule Set (weeks 2–6)

- [ ] Maintain `/movement-log` after every rehearsal
- [ ] Aim for ~8 active rules across the failure-mode catalog tactics relevant to the company's puppets
- [ ] First `/post-show-report` after a public performance
- [ ] First `/peer-review` of a post-show report

### Phase 3 — Backtest and Stabilize (weeks 7–12)

- [ ] Run `/detect-anomaly --backtest` on every active rule monthly
- [ ] Promote rules from `test` to `stable` status as the 30-day-no-tune window completes
- [ ] Refine the failure-mode catalog with any tactic / technique additions discovered in production

### Open Questions

- (e.g. "Do we need a tradition-bearer reviewer for our wayang work, or is in-house puppeteer + rigger sufficient?")
- (e.g. "Should we lift to Tier 3 instrumentation on the lead animatronic to enable bearing-wear rules?")
