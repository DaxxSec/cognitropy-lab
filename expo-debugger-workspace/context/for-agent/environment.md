# Environment Details

> Populated by `/onboard`.

## Local Development

- **OS**: [macOS version]
- **Node.js**: [version — `node -v`]
- **npm / yarn / pnpm**: [which one and version]
- **Xcode**: [version — needed for iOS builds]
- **Expo CLI**: [global or npx]
- **Simulator**: [iOS Simulator version, physical device model if testing on-device]
- **Watchman**: [installed? version?]

## Expo Configuration

- **app.json / app.config.js**: [managed or config plugin-heavy]
- **EAS Project ID**: [if using EAS Build]
- **Expo Account**: [if relevant]

## Railway Backend

- **Project URL**: [Railway dashboard URL]
- **Services**: [list services — e.g., API server, PostgreSQL, Redis]
- **Environment Variables**: [which ones are set — names only, never values]
- **Deploy Method**: [GitHub auto-deploy, manual, Railway CLI]
- **Region**: [e.g., US West]

## Known Environment Quirks

Document anything unusual about the setup here:

```
# Example:
# - Using M1 Mac, some packages need Rosetta
# - Custom metro.config.js for SVG support
# - Monorepo structure with shared packages
```
